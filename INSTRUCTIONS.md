# РКС·Выезд — инструкция пользователя

Краткое руководство для специалиста отдела претензионной работы.
Читать по порядку — каждый раздел отвечает на «как делать вот это конкретно».

---

## 1. Зачем это приложение

Раньше переписку с СПО, ППК и жителями вы вели вручную в Word‑шаблонах: одно дело — 5–12 однотипных писем, каждое требует копировать реквизиты, даты, пункты договора. Это приложение делает следующее:

- хранит **реестр всех обращений** (дел) в одном месте
- генерирует **готовые .docx письма** автоматически — данные подставляются из карточки дела
- считает и инкрементирует **исходящие номера** (`02/0246`, `02/0247`, …)
- хранит **хронологию каждого дела** (что пришло, что отправили, кто выехал, какой ответ)
- следит за **сроками** и подсвечивает горящие дедлайны
- помогает с письмами через **AI‑помощника** (Claude Opus 4.7)

Цель: вместо 30 минут ручной возни с Word — 1–2 минуты на письмо.

---

## 2. Первый запуск — вход

1. Откройте `http://localhost:3030/login` в браузере.
2. В поле e‑mail впишите свой адрес (например `palkov.my@rks-nr.ru`). Пароль не нужен — для прототипа.
3. Список доступных аккаунтов виден на странице входа.
4. После входа попадаете на **дашборд** «Сегодня».

Чтобы выйти — иконка «⎋» в левом нижнем углу рядом с вашим именем.

---

## 3. Дашборд «Сегодня» — что вы видите утром

Открывается по умолчанию (адрес `/`). Здесь сводка дня:

- **Слева вверху** — крупная дата и фраза «У вас N горящих дел, M непривязанных входящих»
- **Справа вверху** — следующий исх.№ (например `02/0249`)
- **4 виджета**:
  - 🟥 **Горящие сроки** — дела, по которым дедлайн просрочен или истекает в ближайшие 3 дня
  - 🟧 **Входящие без дела** — PDF‑ки от ППК/прокуратуры, которые вы загрузили, но ещё не привязали к делу
  - 🟩 **К ответу в ППК** — дела, где замечания устранены и пора ответить заказчику
  - 🟦 **Сегодня выезды** — комиссии, запланированные на сегодня
- **Таблица «В работе сейчас»** — сводка всех активных дел

Клик по любой строке/виджету → попадёте в соответствующий раздел или карточку дела.

---

## 4. Слева — навигация (сайдбар)

Раздел | Что там
---|---
**Сегодня** | Дашборд (то, что вы открыли)
**Дела** | Полный реестр, фильтры (Все / Просрочено / В работе у СПО / К ответу в ППК)
**Входящие** | Письма от ППК/прокуратуры/администрации — PDF + поля
**Реестр исх.** | Все исходящие письма, которые вы сгенерировали
**Календарь** | Выезды комиссии (drag‑n‑drop, экспорт в Outlook/Google)
**Аналитика** | Графики: SLA по СПО, динамика, топ адресов
**МКД** | Справочник адресов
**Контрагенты** | Справочник организаций (ППК, СПО, администрация…)
**Договоры** | Договоры с СПО (пункты, штрафы)
**Пользователи** | Сотрудники с доступом
**Шаблоны** | Каталог 11 типов писем + конструктор

В правом верхнем углу всегда есть:
- поле поиска (или **⌘K** на клавиатуре) — быстро найти дело, объект, исх.№, контрагента
- «Загрузить входящее» — добавить PDF в систему
- «＋ Новое дело» — мастер создания

---

## 5. Главный сценарий: от обращения до ответа в ППК

Это ваш ежедневный рабочий процесс. Шаг за шагом.

### Шаг 1 — пришло письмо от ППК

Вариант **А (вручную)**: на странице «Входящие» кнопка «**＋ Загрузить файл**».
1. Откроется модалка
2. Выберите PDF (с компьютера или с почты)
3. **Поля заполнятся автоматически** через парсер. Если PDF отсканированный или плохо прочитался — есть кнопка «**✨ AI vision**» — она прогоняет файл через Claude и распознаёт точнее
4. Проверьте поля: исх.№, дата, заявитель, источник (Прокуратура / Адм. / Фонд), объект
5. Нажмите «**Сохранить**»

Вариант **Б (автоматически — IMAP)**: если в `.env` настроены IMAP‑креды, кнопка «**↻ IMAP sync**» в `/inbox` сама вытащит непрочитанные письма с домена `ppk-ez.ru`/`mariupol.gov-dpr.ru` и создаст входящие.

После сохранения письмо попадает в раздел **«К обработке»** на `/inbox`.

### Шаг 2 — завести дело из входящего

На карточке входящего (раздел «К обработке») — две кнопки:
- «**Создать дело →**» — открывает мастер из 3 шагов:
  1. Выбрать входящее (предзаполнено)
  2. Выбрать объект МКД и СПО (подтянутся автоматически)
  3. Назначить ответственного (по умолчанию вы)
- «**Привязать к существующему**» — если по этому адресу уже есть открытое дело: модалка с поиском по делам

Дело получит автоматический номер вида `Д‑2026‑0042`.

### Шаг 3 — сгенерировать первое письмо в СПО

Откройте карточку дела. Справа сверху — кнопка «**＋ Сгенерировать ▾**». В дропдауне — **только те шаблоны, которые применимы для текущего этапа** (state machine guard).

На первом этапе доступно:
- **T1 — Акт осмотра МКД** (если планируете выезд)
- **T3 — Письмо в СПО «Об устранении замечаний»** (стандартный первый шаг)

Выбираем **T3**. Открывается модалка:
- **Адресат** — подставится первый контакт СПО
- **Копия** — ППК (Толмачев М.М.)
- **Подписант** — Щербаченя Д.Н. (по умолчанию)
- **Срок ответа СПО** — +30 дней (можно поправить)
- **Дата следующего выезда** — +31 день
- **Тема** — заполнится из карточки
- **Приложения** — список с возможностью добавить/убрать

Справа — **превью A4** с подсвеченными переменными (жёлтые — из БД, синие — редактируемые из формы).

Две кнопки действия:
- «**↓ Скачать .docx**» — мгновенно отдаст файл (без присвоения номера)
- «**Сохранить в дело · присвоить № 02/XXXX**» — присвоит номер, запишет в реестр исходящих, добавит событие в хронологию, переведёт состояние дела

### Шаг 4 — выезд комиссии

Когда наступает срок осмотра, на карточке дела:
- кнопка «**＋ Зафиксировать выезд**»
- открывается форма: дата, тип (первичный / повторный / финальный), итог, состав комиссии, описание, **фото** (на телефоне открывается камера)
- фото сохраняются в `storage/visits/<id>/`, событие пишется в хронологию

Если итог = «**Устранены полностью**» — дело автоматически переходит в состояние «remedy_confirmed» и в дропдауне «Сгенерировать» появится **T5 — Ответ в ППК**.

### Шаг 5 — ответ от СПО (когда пришёл письменный ответ)

На карточке дела — кнопка «**＋ Ответ от СПО**». Форма:
- дата ответа + исх.№ СПО
- **Решение СПО**: «принял» / «обещал график» / «отказ»
- комментарий

В зависимости от решения дело автоматически перейдёт в `awaiting_remedy` или `spo_no_response`.

### Шаг 6 — если СПО не отвечает / не устраняет

Все следующие шаги — это эскалация. На каждом этапе появляются новые шаблоны в дропдауне «Сгенерировать»:

Шаблон | Когда применять | Что подставит
---|---|---
**T6 — Письмо на основании Акта Н/Д** | Когда после повторного выезда замечания не устранены | Ссылку на составленный Акт + срок устранения
**T7 — О гарантиях субподрядчика** | Когда T6 проигнорировали | Пункты гарантий 7.20/7.21, ст. 723 ГК РФ
**T8 — Претензия за непредоставление информации** | Когда СПО не предоставил график / документы | Штраф 150 000 ₽ по п. 8.17

Каждый шаблон при сохранении переводит дело в следующее состояние.

### Шаг 7 — ответ в ППК

Когда замечания устранены (либо вы выбрали «Устранены» в выезде, либо вручную), в дропдауне станет доступен **T5 — Ответ в ППК**. Сгенерируете → сохраните → **«Закрыть дело»** на карточке (кнопка справа сверху).

При закрытии выбираете исход:
- ✅ Замечания устранены
- 🟧 Объёмы работ изъяты у СПО
- 🟥 Передано в арбитраж

Дело уходит из активного реестра, попадает в статистику.

---

## 6. AI‑помощник (Claude Opus 4.7)

Доступен в нескольких местах. Требует `ANTHROPIC_API_KEY` в `.env` (без него выдаёт понятную ошибку).

### Vision‑OCR при загрузке входящего

В модалке «Загрузить файл» рядом с кнопкой выбора PDF — кнопка «**✨ AI vision**».
- Обычный парсер (без AI) работает только с текстовыми PDF
- AI vision работает и со сканами — Claude видит изображение и достаёт поля точнее
- Используйте, если поля заполнились пусто или неправильно

### Резюме дела

На карточке дела, выше хронологии — панель «AI‑помощник» с кнопкой «**✨ Резюме дела**».
- Claude читает карточку + всю хронологию
- Возвращает 3‑5 предложений: что сейчас, что было сделано, что дальше

Удобно после возвращения из отпуска или когда дело передают другому специалисту.

### Чат с делом

Та же панель — кнопка «**💬 Чат с делом**». Открывается модалка с чатом.

Примеры вопросов:
- «Какие сроки сейчас горят?»
- «Когда последний раз СПО что‑то отвечал?»
- «Какие пункты договора применимы для следующего письма?»
- «Какой следующий шаг рекомендуешь?»
- «Сравни этот случай с другими по адресу Карпинского»

Claude помнит весь диалог в рамках треда (тред сохраняется).

### Авто‑драфт абзаца (для сложных писем)

API `/api/ai/autodraft` — даёшь тип шаблона + контекст + цель, получаешь готовый абзац. UI пока скрыт — используется при необходимости разработчиком.

---

## 7. Поиск (⌘K)

Жмёшь **⌘K** (или клик в поисковую строку сверху) — командная палитра.

Ищет одновременно:
- по номерам дел (`Д‑2026‑0042`)
- по адресам (`Металлургов`, `Карпинского 64`)
- по контрагентам (`РКК`, `АудитПроект`)
- по исходящим номерам (`02/0246`)
- по входящим номерам (`ППК‑1‑27290`)

Стрелки ↑/↓ — навигация, ↵ — открыть, **Esc** — закрыть.

---

## 8. Календарь выездов `/calendar`

- 4 вида: месяц / неделя / день / список
- Цвет события = итог последнего выезда:
  - 🟦 индиго — первичный
  - 🟧 амбра — повторный
  - 🟩 мох — устранены
  - 🟥 бордо — дефекты подтверждены
- **Перетащите выезд** на другой день — он автоматически перенесётся (обновится дедлайн в БД)
- Клик по событию → откроется карточка дела
- Кнопка «**↓ Экспорт .ics**» — скачать iCal‑фид и импортировать в Outlook / Google Calendar

---

## 9. Аналитика `/analytics`

5 графиков, обновляются на каждом запросе:

1. **SLA по СПО** — среднее время от создания дела до закрытия по каждому субподрядчику + таблица с количеством просроченных
2. **Динамика по месяцам** — линия количества заведённых дел
3. **Топ адресов** — таблица МКД с наибольшим числом дел (повторяющиеся проблемы)
4. **Нагрузка специалистов** — горизонтальный bar (открыто / всего)
5. **Использование шаблонов** — какие письма генерируются чаще всего

Полезно для отчётности руководству и для разговоров с СПО («у вас 8 просроченных дел против 3 у соседнего подрядчика»).

---

## 10. Реестр исходящих `/outgoing`

Полный список всех писем с автонумерацией. Для каждого письма доступны действия (правая колонка):

Кнопка | Что делает
---|---
**↓ .docx** | Скачать файл документа
**✉ Отправить** | Открыть форму отправки по e‑mail (адресаты подтянуты из карточки документа). Если SMTP настроен — реальная отправка, иначе — в `storage/outbox/`
**✍ Подписать** | Загрузить файл УКЭП (.sig / .p7s / .cms). Документ отметится «УКЭП ✓» с отпечатком SHA‑1
**📡 ЭДО** | Доступна после подписания. Собирает ZIP‑пакет (документ + подпись + opisi.xml) в `storage/edo/`. Готовый адаптер можно подключить к СБИС или Диадок
**↻ статус** | Запросить актуальный статус из ЭДО

В верхнем правом углу страницы — следующий номер исходящего (например `02/0249`).

Кнопка «**↓ Экспорт .csv**» в `/cases` — выгрузить весь реестр дел в Excel.

---

## 11. Справочники (CRUD)

Все справочники одинакового устройства: список → кнопка «＋ Добавить» → форма. У каждой записи кнопка «✎» — редактировать или удалить.

### Контрагенты `/organizations`

Сгруппированы по типу: ППК / СПО / Администрация / Прокуратура / Фонд / Балансодержатель / Мы.

В каждой карточке организации:
- Реквизиты (ИНН/КПП/ОГРН/адрес/email)
- Список **контактов** с тремя формами имени (нужны для шаблонов писем):
  - именительный (Зиаи Данис Айратович)
  - дательный (Заместителю генерального директора · Д.А. Зиаи)
  - звательный (Уважаемый Данис Айратович!)
- Кнопка «＋ контакт» — добавить нового подписанта/представителя
- Кнопка «✎» у контакта — редактировать

### МКД `/buildings`

Адреса с привязкой к СПО и договору. Поиск по фильтру.

### Договоры `/contracts`

С каждым СПО заключён договор. Для шаблонов важно правильно указать пункты:
- Гарантийные обязательства (по умолчанию 7.20, 7.21)
- Порядок устранения (8.10)
- Ответственность (4.1.21)
- Запрос информации (4.1.9)
- Штраф (8.17) + сумма

Эти пункты подставляются в шаблоны T6, T7, T8.

### Пользователи `/users`

Сотрудники с доступом + роли. Только админ может добавлять.

### Шаблоны `/templates`

11 типов писем (T1, T3, T5, T6, T7, T8, …). Каждый имеет:
- Источник (`code` / `builder` / `uploaded`)
- В каких состояниях дела доступен
- В какое состояние переводит

Кнопка «**✎ Редактировать**» открывает редактор с тремя вкладками:

#### Вкладка «Конструктор» — block‑based редактор
- Слева список блоков (шапка, дата+номер, тема, адресат, копии, обращение, абзац, заголовок, приложения, подпись, отступ)
- Стрелки ↑↓ — переставить, ✕ — удалить
- В конце — палитра кнопок «добавить блок»
- Справа — JSON‑превью + текстовое превью
- Внизу «**Сохранить и пересобрать .docx**» — пересобирает файл шаблона в `templates/`

#### Вкладка «Загрузить .docx»
- Если умеете писать шаблон в Word — загрузите готовый файл
- Используйте плейсхолдеры `{ourCompany.ogrn}`, `{addressee.dativeName}`, циклы `{#copies}…{/copies}`
- После загрузки этот файл будет использоваться для генерации

#### Вкладка «Workflow / переменные»
- Список **30+ переменных** с примерами
- Список **2 циклов** (`copies`, `attachments`)
- В каких состояниях дела шаблон доступен

«**Сбросить к дефолту**» — вернуть встроенный шаблон.

---

## 12. Уведомления

### Утренний дайджест
- По умолчанию каждый день в **09:00** автоматически уходит пуш + Telegram: «У вас N горящих, M входящих, K к ответу в ППК, X выездов сегодня»
- Расписание меняется в `.env` → `CRON_DAILY_DIGEST` (формат cron, например `0 9 * * 1-5` — будни)
- Ручной запуск — `POST /api/notifications/digest`

### Push в браузере
- Требуется один раз сгенерировать VAPID‑ключи: `npx tsx scripts/gen-vapid.ts` → скопировать в `.env`
- Включить в браузере: пока через консоль `await window.enablePush()` или через UI‑кнопку (можно добавить позже)
- Работает в фоне даже если браузер закрыт (если приложение установлено как PWA)

### Telegram
- Для отправки в чат указать в `.env`:
  - `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather
  - `TELEGRAM_DEFAULT_CHAT` — ID чата или username канала

---

## 13. PWA (приложение на телефоне)

Открыли `http://your-server.local:3030` на телефоне → меню браузера → «Добавить на главный экран».

Появится иконка приложения. Преимущества:
- Запускается как обычное приложение, без адресной строки
- Получает push‑уведомления в фоне
- В форме «Зафиксировать выезд» поле «Фото» **открывает камеру напрямую** — снять дефект на месте, фото прикрепится к выезду и хронологии
- Часть страниц работает офлайн (благодаря service worker‑кэшу)

---

## 14. Аудит

Каждое значимое действие пишется в `AuditLog`:
- кто (пользователь)
- что (action: `document.generate`, `case.close`, `doc.send` …)
- по чему (entityType + entityId)
- когда (createdAt)
- откуда (IP, user‑agent)

Используется для расследований («кто отправил это письмо?») и для отчётности.

Просмотр через Prisma Studio: `npm run db:studio` → таблица `AuditLog`.

---

## 15. Хоткеи

Клавиша | Действие
---|---
**⌘ K** (Ctrl+K на Win) | Открыть командную палитру (поиск)
**Esc** | Закрыть любую модалку
**↑ ↓** в палитре поиска | Навигация по результатам
**↵** в палитре поиска | Открыть выбранный результат

---

## 16. Если что‑то не работает

Симптом | Что проверить
---|---
AI‑функции возвращают ошибку | `ANTHROPIC_API_KEY` в `.env`, перезапустить `npm run dev`
Push не приходит | Сгенерируйте VAPID `npx tsx scripts/gen-vapid.ts` → впишите в `.env`
Telegram молчит | `TELEGRAM_BOT_TOKEN` и `TELEGRAM_DEFAULT_CHAT` в `.env`; проверьте, что бот в чате
SMTP не отправляет реально | По умолчанию `DEV_FAKE_SEND="true"` → пишет в `storage/outbox/`. Поменять на `"false"` + указать `SMTP_HOST/USER/PASS`
IMAP не тянет | Заполнить `IMAP_HOST/USER/PASS/FOLDER`, доменные ящики (Yandex, Mail.ru) требуют **пароль приложения**, не основной
Не могу войти | Email из `/login` (внизу страницы — список доступных аккаунтов)
Ошибка генерации .docx | `npm run templates:build` пересобрать шаблоны
Сломались данные | `npm run db:reset` → откатывает БД и пересеивает реальными данными из `files/`

---

## 17. Регулярные задачи (рекомендация)

**Утром:**
1. Открыть дашборд → разобрать «Горящие сроки»
2. Привязать «Входящие без дела» к делам или завести новые
3. Если есть выезды — посмотреть `/calendar`

**В течение дня:**
1. После выезда — «＋ Зафиксировать выезд» на карточке дела + фото
2. После любого письма — сгенерировать ответ через «＋ Сгенерировать»
3. После получения ответа от СПО — «＋ Ответ от СПО»

**Вечером:**
1. Закрыть дела, по которым устранили замечания
2. Бегло посмотреть `/analytics` — нет ли СПО, который начал стабильно затягивать

**Раз в неделю:**
1. `/analytics` → топ адресов: если один адрес повторяется — повод поговорить с СПО или эскалировать
2. `/users` → проверить, что нет сотрудников, у которых перегружено

**Раз в месяц:**
1. Экспорт `/cases` → .csv → отчёт руководству
2. Бэкап базы: `cp prisma/dev.db prisma/dev.db.backup-$(date +%Y%m%d)`

---

Если что‑то не описано или работает не так — пишите, поправим.
